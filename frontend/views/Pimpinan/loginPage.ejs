<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <script src="https://cdn.tailwindcss.com"></script>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@10/swiper-bundle.min.css" />
        <script src="https://cdn.jsdelivr.net/npm/swiper@10/swiper-bundle.min.js"></script>    
</head>
<body>
    <div class="flex items-center justify-center min-h-screen">
        <!-- Container -->
        <div class="grid grid-cols-1 md:grid-cols-2 bg-white rounded-lg shadow-lg overflow-hidden w-full flex">
            <!-- Left Section -->
            <div class="flex-1 p-28 flex flex-col justify-center space-y-4">
                <h1 class="text-3xl font-bold text-gray-800">Login</h1>
                <p class="text-gray-600">Silahkan masukkan email dan password anda</p>

                <% if (typeof errorMessages !== 'undefined' && errorMessages.length > 0) { %>
                    <div class="mb-4 text-sm text-red-600 bg-red-100 border border-red-400 p-3 rounded flex items-center">
                        <i class='bx bx-error-circle mr-2'></i>
                        <ul>
                            <% errorMessages.forEach(function(message) { %>
                                <li><%= message %></li>
                            <% }); %>
                        </ul>
                    </div>
                <% } %>
                
                <form class="space-y-4" action="/auth/process-login" method="POST">
                    <div>
                        
                        <label for="nip" class="block text-sm font-medium text-gray-700">NIP</label>
                        <input type="text" id="nip" name="nip" class="mt-1 w-full p-3 border border-gray-300 rounded-lg" placeholder="Masukkan NIP Anda">
                    </div>
                    <div>
                        <label for="password" class="block text-sm font-medium text-gray-700">Password</label>
                        <input type="password" name="password" id="password" class="mt-1 w-full p-3 border border-gray-300 rounded-lg" placeholder="Masukkan Password Anda">
                    </div>
                    <button type="submit" class="w-full bg-orange-400 text-white py-3 rounded-3xl hover:bg-orange-600 transition">Masuk</button>
                </form>
                <p class="text-center text-gray-500">Tidak memiliki akun? <a href="#" class="text-orange-500 font-semibold">Silahkan hubungi TU</a></p>
            </div>
            <!-- Right Section -->
            <div class="flex-1 p-4 flex justify-center items-center">
                <div class="swiper w-full max-w-md">
                    <!-- Wrapper untuk Slide -->
                    <div class="swiper-wrapper">
                        <!-- Slide 1 -->
                        <div class="swiper-slide">
                            <img src="/side_right.png" alt="Slide 1" class="w-full h-auto rounded-lg shadow-md">
                        </div>
                        <!-- Slide 2 -->
                        <div class="swiper-slide">
                            <img src="/side_right.png" alt="Slide 2" class="w-full h-auto rounded-lg shadow-md">
                        </div>
                        <!-- Slide 3 -->
                        <div class="swiper-slide">
                            <img src="/side_right.png" alt="Slide 3" class="w-full h-auto rounded-lg shadow-md">
                        </div>
                    </div>
                    <!-- Pagination (opsional) -->
                    <div class="swiper-pagination"></div>
                </div>
            </div>
        </div>
    
        <!-- Script Swiper -->
        <script>
            const swiper = new Swiper('.swiper', {
                loop: true, // Mengaktifkan looping slide
                pagination: {
                    el: '.swiper-pagination',
                    clickable: true, // Pagination interaktif
                },
                autoplay: {
                    delay: 3000, // Slide otomatis setiap 3 detik
                    disableOnInteraction: false,
                },
                slidesPerView: 1, // Menampilkan 1 slide per tampilan
                spaceBetween: 10, // Jarak antar slide
            });
        </script>
    </body>
    </html>